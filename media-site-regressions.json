{
  "schemaVersion": 2,
  "deprecated": true,
  "sourceOfTruth": "sites/*/regressions.js",
  "note": "Site regression fixtures are colocated with each site profile so site-local failures can be reviewed and changed without editing a shared fixture file."
}
