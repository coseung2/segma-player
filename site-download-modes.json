{
  "schemaVersion": 2,
  "deprecated": true,
  "sourceOfTruth": "sites/*/profile.js",
  "registry": "sites/registry.js",
  "note": "Each site now declares only its downloader, fallback downloader, provider modules, and expected modes in its own profile."
}
