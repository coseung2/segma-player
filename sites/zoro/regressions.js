export const zoroRegressions = Object.freeze([
  Object.freeze({
    id: "zoro-hianime-one-piece-episode-1-player-api",
    liveUrl: "https://hianime.to/watch/one-piece-100?ep=2142",
    recommendedAdblockMode: "on",
    settleMs: 10_000,
    liveOnly: true,
    expected: Object.freeze({
      minimumCandidateCount: 1,
      requireNonAdvertisementPrimary: true,
    }),
  }),
]);
